@using BandsMvc.Models;
@model Band[]
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title></title>
</head>
<body>
    <h3>Alla Band</h3>
    <div>
        <ul>
            @foreach (var item in Model)
            {
                <li>@item.Id @item.Name @item.Description</li>
            }

        </ul>
    </div>
</body>
</html>